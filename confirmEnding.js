// Function: check if a string (first argument, str) ends with the given target string (second argument, target)
function confirmEnding(str) {
    // substr will return the last character in the string
    //  check that the last character in target equals the first letter
    return (str.substr(-target.length) === target);
}
